{
  "builds": [
    {
      "src": "EC.APP/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/EC.APP/browser"
      }
    }
  ],
  "routes": [
    {
      "src": "^/(.*)$",
      "dest": "/index.html"
    }
  ]
}
